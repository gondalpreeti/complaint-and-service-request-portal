# Copy to .env
# Base URL of the FastAPI backend (used by src/api/apiClient.js)
VITE_API_URL=http://localhost:8000


# Optional, defaults to 8000 if unset (used by run.py only, not uvicorn CLI)
PORT=8000